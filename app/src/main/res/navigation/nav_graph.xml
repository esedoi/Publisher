<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/FirstFragment">

    <fragment
        android:id="@+id/FirstFragment"
        android:name="com.paul.publisher.FirstFragment"
        android:label="@string/first_fragment_label"
        tools:layout="@layout/fragment_first">


    </fragment>

    <dialog
        android:id="@+id/inputDialog"
        android:name="com.paul.publisher.InputDialog"
        android:label="InputDialog" />
    <action
        android:id="@+id/action_global_inputDialog"
        app:destination="@id/inputDialog" />

</navigation>